import { Filter.NamesPipe } from './filter.names.pipe';

describe('Filter.NamesPipe', () => {
  it('create an instance', () => {
    const pipe = new Filter.NamesPipe();
    expect(pipe).toBeTruthy();
  });
});
